import { Feature, FeatureTitle } from './components';

export const Features = () => {
    return (
        <div style={{ marginBottom: '100px' }} className="wrapper" id='features'>
            <FeatureTitle />
            <Feature />
        </div>
    );
};
